//! The edge binary.
//!
//! Will own a `SimulatorEdge`: the sim-to-edge protocol on one side, client
//! connections on the other. That type does not exist yet, so neither does this
//! program.

fn main() {
    println!("herd-edge: placeholder. SimulatorEdge is phase 8 of umwelt's build order.");
}
